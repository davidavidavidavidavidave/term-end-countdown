<%= form_with(model: countdown) do |form| %>
  <% if countdown.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(countdown.errors.count, "error") %> prohibited this countdown from being saved:</h2>

      <ul>
        <% countdown.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
<div id="countdowns">
      <div class="card-trip">
        <div class="card-trip-infos">
          <div>
            <div>
              <div class = "flex-fill">
                <h2><%= form.label :name, style: "display: block" %>
                :
                <%= form.text_field :name %></h2>
              </div>
                <br>
              <div>
                <h2><%= form.label :date, style: "display: block" %>
                 :
                <%= form.datetime_field :target %></h2>
              </div>
                <br>
              </div>
            <div>
              <h2><%= form.submit :data => {:action => 'click->countdown#handle_click'}, class: "new-style"%></h2>
            </div>
          <% end %>
          </div>
        </div>
      </div>
    </div>
